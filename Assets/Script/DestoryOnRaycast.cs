using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestoryOnRaycast : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //RaycastHit ScreenPointTorRay;
        //Ray ray = Camera.main.ScreenPointTorRay(Input.mousePosition);
    }
}
